services:
  - type: web
    name: xhunter-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k gevent -w 1 -b 0.0.0.0:10000 server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
