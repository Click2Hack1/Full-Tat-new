services:
  - type: web
    name: xhunter-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 -b 0.0.0.0:10000 server:app
    runtime: python311
    envVars:
      - key: PORT
        value: 10000
